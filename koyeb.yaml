app:
  name: ryx-dashboard
  
build:
  docker:
    dockerfile: Dockerfile
    
service:
  ports:
    - port: 5000
      protocol: http

  routes:
    - path: /
      port: 5000
  
  env:
    - key: PYTHONUNBUFFERED
      value: "1"
    - key: DATABASE_URL
      value: "postgresql://unble:npg_1kjV0mhECxqs@ep-divine-bird-a1f4mly5.ap-southeast-1.pg.koyeb.app/unble"
  
  scaling:
    min: 1
    max: 1
  
  health_checks:
    - http:
        path: /api/status
        port: 5000
      interval: 30
      timeout: 10
      
  regions:
    - sin